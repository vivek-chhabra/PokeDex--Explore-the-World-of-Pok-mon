import { createSlice } from "@reduxjs/toolkit";
import { AppSlice } from "../../Types/interfaces";



const initialState: AppSlice = {}

const appSlice = createSlice({
    name: 'app',
    initialState,
    reducers: {

    }
})

